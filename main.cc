#include <stdio.h>

#include "module.h"

int main()
{
    printf("The flower of Apricot\n");
    printf("Factorial(5): %d\n", Factorial(5));
    printf("IsPrime(5): %d\n", IsPrime(5));
    return 0;
}
